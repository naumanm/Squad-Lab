<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Index</title>
</head>
<body>
<h3>Squads</h3>
  <% i = 1 %>
  <a href="/squads/new">Add a new Squad</a>

  <% @squads.each do |squad| %>
    <a href="/squads/<%= i %>">
      <h4><%= squad['name'] %> <%= squad['mascot'] %></h4>
    </a>

    <p>
      <form action="/squads/<%= i %>" method="POST">
        <input type="submit" value="Show squad">
      </form>
    </p>

    <% i += 1 %>
  <% end %>  

</body>
</html>
